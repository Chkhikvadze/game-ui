query walletByPlayer($player_id: ID!) @api(name: project) {
  walletByPlayer(player_id: $player_id) {
    id
    wallet_type
    source
    label
    address
    protocol
    network
    created_on
    modified_on
    created_by
  }
}
