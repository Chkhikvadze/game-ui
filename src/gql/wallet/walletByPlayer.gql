query walletByPlayer($player_id: ID!) @api(name: game) {
  walletByPlayer(player_id: $player_id) {
    id
    wallet_type
    source
    label
    address
    protocol
    network
    balance
    created_on
    modified_on
    created_by
  }
}
