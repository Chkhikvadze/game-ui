query walletsByCurrentUser($filter: ListFilterQuery) @api(name: project) {
  walletsByCurrentUser(filter: $filter) {
    items {
      id
      wallet_type
      source
      label
      address
      protocol
      network
      created_on
      modified_on
      created_by
    }
    total
    page
    limit
  }
}
