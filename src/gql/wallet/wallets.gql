query wallets($filter: ListFilterQuery) @api(name: project) {
  wallets(filter: $filter) {
    items {
      id
      wallet_type
      source
      label
      address
      protocol
      network
      created_on
      modified_on
      created_by
    }
    total
    page
    limit
  }
}
