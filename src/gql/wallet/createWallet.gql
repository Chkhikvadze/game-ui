mutation createWallet($input: WalletInput!) @api(name: project) {
  createWallet(input: $input) {
    wallet {
      id
      wallet_type
      source
      label
      address
      protocol
      network
      created_on
      modified_on
      created_by
    }
    success
    message
  }
}
