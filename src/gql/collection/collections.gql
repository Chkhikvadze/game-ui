#import "./collectionFragment.gql"

query collections($filter: CollectionFilterQuery) @api(name: project) {
  collections(filter: $filter) {
    items {
      ...CollectionFields
    }
    total
    page
    limit
  }
}
