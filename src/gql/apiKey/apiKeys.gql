query apiKeys($filter: ListFilterQuery) @api(name: project) {
  apiKeys(filter: $filter) {
    items {
      id
      name
      token
      note
      expiration
      projects
    }
    total_count
  }
}
