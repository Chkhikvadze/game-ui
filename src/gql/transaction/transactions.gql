query transactions($filter: TransactionFilterQuery) @api(name: game) {
  transactions(filter: $filter) {
    items {
      id
      contract_address
      from
      to
      blockchain
      chain_name
      transaction_hash
      events
      method
      created_on
      transaction_assets {
        id
        amount
        asset {
          id
          token_id
          name
          asset_type
          medias
        }
      }
    }
  }
}
