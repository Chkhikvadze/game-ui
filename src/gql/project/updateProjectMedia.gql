#import "./projectFragment.gql"

mutation updateProjectMedias($id: ID!, $input: [MediaInput!]!) @api(name: game) {
  updateProjectMedias(id: $id, input: $input) {
    success
    game {
      ...ProjectFields
    }
  }
}
