query projectById($id: ID!) @api(name: project) {
    projectById(id: $id) {
        id
        name
        category
        description
        created_on
        modified_on
        created_by
        modified_by
    }
}
