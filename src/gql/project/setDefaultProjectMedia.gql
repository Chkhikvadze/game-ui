#import "./projectFragment.gql"

mutation setDefaultProjectMedia($game_id: ID!, $media_id: ID!) @api(name: game) {
  setDefaultProjectMedia(game_id: $game_id, media_id: $media_id) {
    success
    game {
      ...ProjectFields
    }
  }
}
