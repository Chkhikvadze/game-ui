mutation changePassword($currentPassword: String!, $password: String!) @api(name:account)  {
  changePassword(currentPassword: $currentPassword, password: $password) {
      message
      success
  }
}
