mutation resendVerifyEmail($body: JSON!) {
  resendVerifyEmail: publish(body: $body) @rest(
      type: "Post"
      path: "/auth/resend-verify-email"
      method: "POST"
      bodyKey: "body",
      endpoint: "project"
    ) {
      message
      success
  }
}
