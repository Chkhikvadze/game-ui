mutation resendCode($body: JSON!) {
  resendCode: publish(body: $body) @rest(
      type: "Post"
      path: "/auth/resend-code"
      method: "POST"
      bodyKey: "body",
      endpoint: "project"
    ) {
      message
      success
  }
}
