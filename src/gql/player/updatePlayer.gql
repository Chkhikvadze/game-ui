mutation updatePlayer($id: ID!, $input: PlayerInput!) @api(name: project) {
  updatePlayer(id: $id, input: $input) {
    success
    player {
      id
      name
      unique_id
      avatar
      custom_props
      last_seen
      created_on
      modified_on
      created_by
      modified_by
      account_id
      project_id
    }
  }
}
