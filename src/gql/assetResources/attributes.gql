query attributes($filter: AttributeFilterQuery) @api(name: game) {
  attributes(filter: $filter) {
    items {
      id
      name
      min
      max
      game_id
      account_id
      created_on
      modified_on
      created_by
      modified_by
      description
      order
      media
    }
    total
    page
    limit
  }
}
