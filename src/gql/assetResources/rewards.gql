query rewards($filter: RewardFilterQuery) @api(name: game) {
  rewards(filter: $filter) {
    items {
      id
      name
      game_id
      account_id
      created_on
      modified_on
      created_by
      modified_by
      description
      order
      media
    }
    total
    page
    limit
  }
}
