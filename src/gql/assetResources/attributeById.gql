query attributeById($id: ID!) @api(name: game) {
  attributeById(id: $id) {
    id
    name
    min
    max
    game_id
    account_id
    created_on
    modified_on
    created_by
    modified_by
    description
    order
    media
  }
}
