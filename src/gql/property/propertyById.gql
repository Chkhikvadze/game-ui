query propertyById($id: ID!) @api(name: game) {
  propertyById(id: $id) {
    id
    name
    game_id
    account_id
    created_on
    modified_on
    created_by
    modified_by
    parent_id
    config
    description
    status
    property_type
    collection_id
  }
}
