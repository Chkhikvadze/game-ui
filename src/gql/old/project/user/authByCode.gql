mutation authByCode($body: JSON!) {
  authByCode: publish(body: $body) @rest(
      type: "Post"
      path: "/auth/code"
      method: "POST"
      bodyKey: "body",
      endpoint: "project"
    ) {
      accessToken
      refreshToken   
      exp
  }
}
