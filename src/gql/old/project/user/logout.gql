mutation logout($body: JSON!) {
  logout: publish(body: $body) @rest(
      type: "Post"
      path: "/auth/logout"
      method: "POST"
      bodyKey: "body",
      endpoint: "project"
    ) {
      success
  }
}
